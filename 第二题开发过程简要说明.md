# 开发过程简要说明 — 上位机调试（SG90 舵机 + VOFA+ 可视化）

> 考核题：软件第二题《上位机调试》
> 目标：STM32F103C8T6 驱动 SG90/MG90S 舵机，用 VOFA+ 完成“发角度 → 舵机响应 →
> 回传角度+PWM占空比 → JustFloat 可视化”。
> 本说明对应工程目录 test2/ 下代码（Core/Src：main.c、servo.c、tim.c、usart.c 等）。

---

## 1. 题目要求与本工程的完成情况

| # | 题目要求 | 完成情况 | 对应代码/操作 |
| 1 | 从 VOFA+ 发目标角度，舵机能响应 | ✅ | USART1 中断逐字节解析 `#xxx` → `servo_set_angle()` |
| 2 | STM32 实时回传“当前舵机转动角度”和“PWM 占空比” | ✅ | 每 20ms 上报一帧（角度 90° + 占空比 7.5% 等） |
| 3 | 按 VOFA+ JustFloat 协议格式化，波形可视化 | ✅ | `vofa_justfloat_send()`：2×float + 帧尾 `00 00 80 7F` |


---

## 2. 系统组成与硬件连接

| 项目 | 内容 |
|---|---|
| 主控 | STM32F103C8T6（蓝丸/最小系统板，HSE 8MHz → PLL×9 = 72MHz） |
| 舵机 | SG90（180° 型），PWM 控制，0.5ms~2.5ms ↔ 0°~180° |
| 信号 | PA6 = TIM3_CH1（PWM，50Hz），AF 推挽输出 |
| 舵机供电 | 独立 5V（12V 电池经降压模块），**与单片机共地**（排查中验证了共地的重要性） |
| 上位机串口 | USART1（PA9=TX / PA10=RX），115200-8-N-1，板载 CH340 / USB-TTL |
| 上位机 | VOFA+（RawData 调试 → JustFloat 可视化） |

接线示意：
```
舵机 红线→5V(+)    棕线→GND(与单片机共地)    橙线→PA6(TIM3_CH1)
STM32  PA9/PA10 ↔ USB-TTL/CH340 → PC VOFA+(COM, 115200)
```

---

## 3. 通信协议设计

### 3.1 帧结构

| 方向 | 格式 | 示例 | 说明 |
|---|---|---|---|
| PC → STM32 | `#`+3 位十进制角度(+可选 `\n`) | `#090`、`#90\n` | 凑满 3 位数字或收到 `\n` 即生效，范围 0~180，越界/坏帧整帧丢弃 |
| STM32 → PC(阶段2,文本) | `angle=xx duty=yy%` | `angle=90 duty=7%` | 开发期调试用，RawData 查看 |
| STM32 → PC(阶段3,JustFloat) | 2×float(小端)+帧尾 `00 00 80 7F` | —— | VOFA+ 协议选 JustFloat，自动拆 2 通道画波形 |

- 通道 0 = 当前角度（按指令角度回显，舵机为位置闭环，到位后当前角=目标角）
- 通道 1 = PWM 占空比（定时器实际写入值回显）

### 3.2 脉宽映射公式（舵机控制核心）

```
pulse_us = 500 + angle/180 × (2500 - 500)      // 0°→0.5ms, 90°→1.5ms, 180°→2.5ms
duty%    = pulse_us / 20000 × 100%             // 周期 20ms
```

### 3.3 定时器参数（50Hz PWM 的产生）

| 参数 | 值 | 由来 |
|---|---|---|
| 系统时钟 | 72MHz | HSE 8MHz × PLL 9 |
| APB1 | 36MHz | ÷2 |
| TIM3 计数时钟 | 72MHz | APB1 定时器倍频 ×2 |
| PSC | 71 | → 1MHz，1 计数 = 1µs（比较值直接等于脉宽 µs） |
| ARR | 19999 | → 周期 20000µs = 20ms = **50Hz** |
| CCR1 | 500~2500 | 与 0°~180° 对应（实测回中 CCR1=1500） |

---

## 4. 软件结构与关键点

- **servo.c / servo.h**：`servo_attach()`（补配 PWM 通道 PWM1 + `HAL_TIM_PWM_Start`，上电回中 90°）、`servo_set_angle()`（角度钳位 0~180 + 脉宽映射 + 写比较值）、角度/占空比读取函数。
- **main.c**：
  - 串口逐字节中断接收 + 帧解析（`HAL_UART_RxCpltCallback`）：`#` 起帧、3 位数字封帧、`\n` 兜底，非法/越界整帧丢弃；
  - 主循环：收到指令 → `servo_set_angle()`；每 20ms → 按宏 `VOFA_PROTOCOL_JUSTFLOAT` 选择 JustFloat/文本上报；
  - 上报函数：2 个 float 小端 + 帧尾 `00 00 80 7F`，每帧 12 字节。
- 上电自检（临时段，用于硬件排查，现已可删）：上电后舵机 90°→0°→180°→90° 匀速扫描，不依赖上位机即可肉眼确认 PWM/供电/接线。

---

## 5. 开发过程与遇到的问题

按阶段推进：先让舵机转起来（阶段 0）→ 文本收发（阶段 1）→ JustFloat 可视化（阶段 2）。

### 问题 1：舵机上电完全不动、无声
- 现象：烧录后上电舵机无任何动作。
- 排查：先怀疑固件未真正写入。检查发现下载器/芯片组合会触发 ST 官方工具的**正版校验**（GDB 检查不过 / `could not connect (error 138)`），判断固件可能从未成功写入。
- 解决：改用 **OpenOCD** 烧录（OpenOCD 走纯 SWD，不做正版校验）。成功后 OpenOCD 报告 `** Programming Finished ** / ** Verified OK **`。
- 佐证：SWD DPIDR 读出 `0x2ba01477`（标准 STM32F1 为 `0x1ba01477`，`0x2b…` 为克隆芯片调试块特征，与正版校验失败互相印证）。
- 引申：日常开发用 OpenOCD 烧录调试；如需 ST 官方工具（如验收录屏），需更换正版 ST-Link。

### 问题 2：烧录成功后舵机仍不动
- 用 OpenOCD 读取运行中芯片寄存器验证固件执行状态
  - `GPIOA CRL = 0x4a444444` → PA6 复用推挽输出 ✓
  - `TIM3 CR1 = 0x81`（计数器已启动）、`CCMR1 = 0x68`（PWM1 模式）、`CCER = 0x01`（通道使能）✓
  - `CCR1 = 0x5DC = 1500`（1.5ms=90°）、`ARR = 0x4E1F = 19999`（20ms=50Hz）、`PSC = 0x47 = 71` ✓
- 结论：代码侧 PWM 已正确输出，问题定位到**物理层（接线/供电/舵机本体）**。
- 进一步验证：临时加入“上电自检扫描”代码，舵机可转 → 确认 MCU→PA6→舵机链路通；曾出现的“完全不动”源于某根信号线未插实。

### 问题 3：舵机供电不足导致卡顿/乱摆/上电抖动
- 现象：动作一顿一顿、上电瞬间乱摆；测量舵机端电压：**上电瞬间掉到 2.5V**，运行期仅 4.6~4.75V（低于 SG90 标称 4.8V）。
- 原因：SG90 启动/换向瞬时电流可达 600mA+，电源给不出瞬时电流 → 电压跌落 → 力矩不足。
- 解决：
  - 降压模块输出在**带载状态**下调准到 5.0~5.2V；
  - 模块→舵机接线插紧；
  - 实测改善后：运行期电压稳定 **≈4.8V**。
- 教训：SG90 必须独立足流供电并共地，不能靠单片机/ST-Link 3.3V 供电。

### 问题 4：转动“涩、一顿一顿”
- 现象：自检扫描时动作发涩、不流畅。
- 原因分析：测试程序用 1°/8ms 的微步长扫描，步长落在 SG90 电位器死区（约 0.5~1°）附近，舵机内部反复“追不准”，表现为低速摩擦感；且指令平均速度偏低。
- 解决：步长加大到 5°/15ms（约 330°/s，明显大于死区）后转动连续顺滑；模拟舵机“一步到位甩动”属正常响应，连续平滑须配合连续小角度指令。
- 静止自检：舵机停在目标角度不抖、无嗡嗡声 → 信号与供电在稳态下正常。


### 问题 5：VOFA+ 里全是十六进制/没波形
- 原因：协议停在 RawData（hex 查看），或舵机恒停在 90° 曲线为水平线看似“无波形”。
- 解决：协议切 **JustFloat** → 波形区出现 Ch0/Ch1；发送 `#000→#045→#090→#135→#180` 观察阶梯波形。
- 数据自检：收到 `00 00 B4 42 | 01 00 F0 40 | 00 00 80 7F` = 角度 90.0 + 占空比 7.5% + 帧尾，格式正确。

---

## 6. 验证结果

| 验证项 | 结果 |
|---|---|
| PWM 输出 | PA6 50Hz，脉宽 1.5ms@90°（寄存器实测 CCR1=1500/ARR=19999） |
| 上电回中 | 上电舵机回到 90° 并保持 |
| 串口指令 | `#000/#090/#180` 舵机正确到位，越界(>180)与坏帧不执行 |
| 回传 | 每 20ms 一帧 JustFloat，含角度与占空比 |
| 可视化 | VOFA+ JustFloat 双通道波形随指令呈阶梯变化 |
| 供电 | 运行期 ≈4.8V，加电容后无明显跌落 |

---

## 7. 提交物清单与录像脚本（供参考）

- [ ] 工程源码（CubeMX 工程 + Core 代码，剔除 build 中间产物）
- [ ] 本开发说明
- [ ] 实操视频（横屏，画面同时含硬件接线与电脑屏幕）：
  1. 文本阶段：RawData 看到上电提示 `servo ready, send #000~#180`，发 `#090/#000/#180` 看舵机响应与回显；
  2. JustFloat 阶段：切 JustFloat，连发 `#000→#045→#090→#135→#180`，波形呈阶梯上升；
  3. 展示供电方案（独立 5V + 共地）与问题排查结论（供电不足→卡顿，OpenOCD 烧录兼容克隆硬件）。

> 备注：本项目调试器为克隆 ST-Link 且芯片为克隆片，ST 官方工具触发正版校验无法连接，
> 日常烧录调试统一使用工程内 flash/ 目录的 OpenOCD 脚本（flash.ps1 / probe.ps1 / debug.ps1）。
